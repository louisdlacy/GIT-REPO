import { SpawnPointGizmo } from "horizon/core"


const spawnPoints: SpawnPointGizmo[] = [];
let lastSpawnPointIndex = -1;


export const randomSpawnPoint_Data = {
  spawnPoints,
  lastSpawnPointIndex,
}